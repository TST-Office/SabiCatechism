import theme, { COLORS, SIZES, FONTS } from './theme'
import images from './images'
import colors from './colors'
import data from './data'

export { images, theme, COLORS, SIZES, FONTS, colors, data }